CREATE TABLE "invoices" (
    'id' BLOB PRIMARY KEY CHECK (is_uuid_v7(id)) NOT NULL,
    'name' TEXT NOT NULL DEFAULT '',
    'numberSeries' BLOB REFERENCES 'number_series'('id'),
    'party' BLOB REFERENCES 'parties'('id'),
    'account' BLOB REFERENCES 'accounts'('id'),
    'date' TEXT NOT NULL DEFAULT '',
    'priceList' BLOB REFERENCES 'price_lists'('id'),
    'netTotal' REAL NOT NULL DEFAULT 0,
    'baseGrandTotal' REAL NOT NULL DEFAULT 0,
    'grandTotal' REAL NOT NULL DEFAULT 0,
    'setDiscountAmount' INTEGER NOT NULL DEFAULT 0,
    'discountAmount' REAL NOT NULL DEFAULT 0,
    'discountPercent' REAL NOT NULL DEFAULT 0,
    'entryCurrency' TEXT NOT NULL DEFAULT '',
    'currency' BLOB REFERENCES 'currencies'('id'),
    'exchangeRate' REAL NOT NULL DEFAULT 0,
    'discountAfterTax' INTEGER NOT NULL DEFAULT 0,
    'makeAutoPayment' INTEGER NOT NULL DEFAULT 0,
    'makeAutoStockTransfer' INTEGER NOT NULL DEFAULT 0,
    'outstandingAmount' REAL NOT NULL DEFAULT 0,
    'stockNotTransferred' REAL NOT NULL DEFAULT 0,
    'terms' TEXT NOT NULL DEFAULT '',
    'attachment' TEXT NOT NULL DEFAULT '',
    'isReturned' INTEGER NOT NULL DEFAULT 0,
    'isFullyReturned' INTEGER NOT NULL DEFAULT 0,
    'isSynced' INTEGER NOT NULL DEFAULT 0,
    'created' TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP CHECK (datetime(created) IS NOT NULL),
    'updated' TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP CHECK (datetime(updated) IS NOT NULL)
) STRICT;